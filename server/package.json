{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "dev": "npx json-server db.json --port 5000 --watch"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
