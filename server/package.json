{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "start": "npx json-server db.json --port $PORT --host 0.0.0.0 --middleware cors.json",
    "dev": "npx json-server db.json --port 5000 --watch"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
